use pokemon;

create table iniciales(
nombre varchar(30),
region varchar(30),
numeroPokedex int(5),
tipo varchar(50),
debilidad varchar(50),
imagen varchar(150)

);
select * from iniciales;
drop table iniciales;